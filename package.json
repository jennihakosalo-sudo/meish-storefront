{
  "name": "meish-storefront",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Meish Printed — editorial storefront (MVP foundation)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "astro build && wrangler pages deploy dist --project-name=meish-storefront"
  },
  "dependencies": {
    "@fontsource-variable/fraunces": "^5.1.0",
    "@fontsource-variable/inter": "^5.1.0",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
